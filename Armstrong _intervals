low=int(input ("Enter the lowest interval: "))
high=int(input ("Enter the highest interval: "))
for i in range(low+1,high):
  sum=0
  a=[int(x) for x in str(i)]
  length=len(a)
  for j in a:
    b=j**length
    sum+=b
  if i==sum:
    print(i)
